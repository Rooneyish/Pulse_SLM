[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pulse_slm"
version = "0.1.0"
dependencies = [
    "datasets>=5.0.0",
    "tokenizers>=0.23.1",
    "tqdm>=4.69.0",
]
requires-python = ">=3.11"
